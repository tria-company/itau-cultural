# S1 · Admin

sessão: bid-ita-d4
estado: rodando
tarefa: 11 de 11 — todas entregues
pasta: `(bastidor)/admin/`
prd: [`sessoes/S1-ADMIN.md`](../sessoes/S1-ADMIN.md)

> **Só a S1 escreve neste arquivo.** A sessão de controle lê e agrega no `PAINEL.md`.
> Uma entrega só entra na tabela **com hash de commit**. Sem hash, não aconteceu.

## Entregas

| # | tarefa | commit | quando | nota |
|---|---|---|---|---|
| 1 | folha `admin.css` + linha no `globals.css` | 60030ae | 25.08 23:00 | 125 linhas, não 400: superfície, painel, tabela, pastilha e a caixa de declaração honesta vêm de `studio.css`; a folha traz só o bloco de parâmetro. **O hash não é meu** — a S6 varreu `admin.css` para o commit dela; ver Divergências |
| 2 | `src/dados/admin.ts` — DTOs do motor, territórios e observabilidade | 1bf249a | 25.08 23:48 | `CustoDoParametro` é união discriminada: não existe parâmetro sem custo medido **ou** declaração de que não foi. Três dos quatro declaram não-medido. DTO de 6.554 bytes, conferência de três pontas fechando com zero divergências. Achado: a concentração é 59,2% de 773 **registros**, com 718 entidades distintas por trás — o PRD funde os dois |
| 3 | **A2** motor — `(bastidor)/admin/motor` | 7d0490d | 25.08 23:58 | Fixa o padrão da sessão: valor à esquerda, medição à direita, sempre visível. Ganhou escrita — mudar parâmetro exige valor novo **e** motivo, botão desabilitado até valer, registro com autor e carimbo, sem apagar. Armazenamento bloqueado virou frase na tela em vez de `catch` vazio. `studio.css` cobriu formulário, tabela e painéis: a folha do Admin não cresceu. **Ainda não aberta em navegador** — dev parado por ordem da coordenação |
| 3b | A2 vista no navegador — três defeitos corrigidos | 602b7e5 | 26.08 00:22 | Campos do formulário não pareciam campos; coluna da medição esticava até 1.100px com a régua atravessando um terço vazio; ids dos concentradores quebrando no meio da palavra. Fluxo conferido de ponta a ponta: botão desabilitado dizendo o que falta → habilita → registra com autor e carimbo → sobrevive ao recarregamento. Visão app esconde e mostra o aviso, D-67 ok. **Os três passavam nos portões estáticos** |
| 4 | **A3** territórios — `(bastidor)/admin/territorio` | a07801c | 26.08 01:05 | A alavanca virou operável: as origens de centroide-país são **cidades fora da tabela** — Paris 20, Nova York 19, Lisboa 8 —, e a tela diz quantas entidades cada uma move. Três achados: `meta.json` conta a **tabela** (472), o mapa resolve **1.380**, e a fatia de centroide-país é 45,3% numa e 29,1% na outra; origem sem território acima **é o país** (66 entidades presas, onde município não resolve); cidade dentro de homônimo lê como defeito e some. A trilha virou lista única com união discriminada, chave `admin.v2`. Dois defeitos de forma vistos só no navegador: régua do `.studio-acoes` em célula de tabela, e `45.3%` com ponto |
| 5 | **A6** observabilidade — `(bastidor)/admin/observabilidade` | e510b79 | 26.08 01:30 | Primeira tela do Admin **inteiramente de servidor**: sem estado, sem armazenamento, zero JS no navegador. A conferência de três pontas passa a aparecer **antes** de derrubar o build, com o nome da fatia. Frescor sem relógio. Três defeitos vistos só no navegador: crase de código vazando para a frase, procedência saindo como enum cru (`ic`), caixa repetindo o próprio rótulo. Reusei `ROTULO_DA_PROCEDENCIA` do Observatório para as duas superfícies não batizarem a mesma fatia diferente |
| 6 | **A1** papéis e escopos — `(bastidor)/admin/papeis` | ab6acb6 | 26.08 01:55 | O formulário **não tem campo de procedência**: escolhe-se o papel e a tela diz qual carimbo ele autoriza. Eixos de escopo aparecem só para moderação; para os outros a tela explica por que não aparecem. Matriz de autoria **viva**, conferida contra `meta.json` a cada build — `programa` aparece com zero e a frase. As quatro procedências futuras trazem «a produção abre», não zero. Defeito de forma visto no navegador: campos esticando a 1.350px |
| 7 | **A7** trilha de auditoria — `(bastidor)/admin/auditoria` | 079007f | 26.08 02:20 | Zero ações de escrita, e a varredura dos botões no navegador confirma: só os quatro filtros e o alternador de visão da casca. O estado vazio são **três** estados — «ainda não li», «trilha vazia», «filtro não tem nada» —, porque numa tela de auditoria dizer «nenhum registro» com o filtro fechado é afirmar sobre o sistema algo que a tela não sabe. Sem reordenação por coluna: a sequência é o conteúdo |
| 8 | **A4** vocabulário · **A5** limites da IA | da62552 | 26.08 02:45 | A4 é tela de servidor com **zero ações** (varredura de botões confirma). A5 dá dono à lista que já existia no módulo da Moderação, que a aplica. Duas ausências conferidas por sonda: **nenhum checkbox ou `role=switch`** na página — não existe interruptor de «IA publica direto» — e **nenhuma ação de remover** limite. A trilha ganhou o quarto tipo de evento e o quarto filtro na A7 |
| 9 | **A8** titulares · **A9** governança operacional | f40e0dd | 26.08 03:20 | A8 separa **usuária** de **retratada** — 575 pessoas no grafo nunca se cadastraram e o direito delas não depende disso. Contestação de verbete é encaminhada à Enciclopédia, não resolvida aqui; escolher «Exclusão» abre o aviso do que ela leva junto **antes** de confirmar. A9 declara os quatro poderes e **não oferece controle para nenhum** (varredura confirma zero botões): um «Suspender» decorativo ensinaria que suspensão é cosmética. No meio disto o disco bateu em **76 MiB livres** e a sonda morreu em `write` — apaguei o `.next-s1`, 668 MB, e voltei |
| 10 | **A10** desempenho da moderação | 4da0156 | 26.08 03:40 | **Nenhuma das cinco medidas fecha**, e a tela declara em vez de exibir cinco zeros — «tempo médio de fila: 0» afirmaria fila instantânea quando o fato é que os 68 itens nunca foram decididos. A quinta é medível pela metade: a densidade existe, o tempo de fila não. O recorte contra vigilância está impresso: mede a fila, não a pessoa |
| 11 | Suíte `scripts/verificar-admin.mjs` | a569ef4 | 26.08 04:05 | **8 gates**, nenhum dependente de build: as regras são sobre código e dado, e verificação que só roda com disco livre e fila vazia é verificação que não roda. **Cada gate testado com o defeito injetado** — e um estava mentindo: `onClick={() => apagarRegistros()}` com rótulo «Confirmar» passava verde, porque `\b` no fim do padrão não casa «apagar» seguido de «R». Era exatamente a forma que o gate existe para pegar. Corrigido e reprovado |

**A nota é obrigatória**: uma linha sobre o que foi feito, o que ficou de fora, ou o que
surpreendeu. O hash diz que aconteceu; a nota diz o quê.

## Bloqueios

**B-01 · RESOLVIDO em 25.08 23:55.** O `tsc --noEmit` estava vermelho por arquivos de S2,
S3 e S5 não commitados; hoje o repositório inteiro compila limpo. `npm run checar` está no
estado declarado do protocolo: 40 verdes e só a falha herdada de `cursos.css`.

**B-03 · `npm run build` nunca rodou nesta sessão.** O portão 1 do §9 pede build verde e
export estático completo. A vez de build ficou com a S3 a noite toda e o disco chegou a
**76 MiB livres**. As dez telas foram abertas em `next dev` e medidas no navegador, e a
suíte nova não depende de build — mas «export estático completo» é afirmação que eu **não
posso fazer**. Destrava com a vez de build concedida e disco.

**B-02 · RESOLVIDO em 26.08 00:22.** Vez de dev concedida, A2 aberta na 3001, três defeitos
achados e corrigidos em `602b7e5`, servidor encerrado. A lição vale para as próximas nove
telas: **os três passavam em `tsc` e em `verificar-ds`** — portão estático não vê campo sem
moldura, coluna esticada nem id quebrado ao meio. Toda tela desta sessão vai ser aberta
antes de eu chamá-la de pronta.

## Pedidos de contrato

—

## Divergências

**D-01 · Duas sessões commitaram arquivos meus.** `@import "../estilos/admin.css"` entrou no
repositório dentro de `4a14e13` (S6), e a folha `src/estilos/admin.css` dentro de `60030ae`
(S6, mensagem «a S6 registra a tarefa 1»), que levou junto quatro arquivos do Observatório,
da S2. Nenhum arquivo meu foi alterado — o conteúdo em HEAD é byte a byte o que escrevi —,
mas a tarefa 1 não tem commit meu, e o §3 do protocolo pede commit atômico por sessão.
Registrado, não desfeito: reverter apagaria trabalho de duas sessões.

**D-02 · `fanoutEfetivo` é 20, igual ao teto.** O PRD manda a A2 explicar por que o fanout
efetivo cai abaixo do teto quando a fonte é grande; em `meta.json` os dois são 20, e neste
acervo ele não cai. A A2 vai imprimir o mecanismo como possível e declarar que não se
manifestou neste build, em vez de repetir a prosa contra o dado — é ausência declarada com
denominador, a regra da casa. Decidido com a sessão de controle; não vira PEDIDO.

**D-03 · A A2 terá um parâmetro medido e três declarações de «não medido».** Só o
`LIMIAR_PROBABILISTICO` tem alternativo medido em código (`LIMIAR_ALTERNATIVO_MEDIDO`, em
`duplicatas.ts:112`). `grauHub`, `fanoutSemelhante` e a dose de serendipidade nascem com a
declaração de que o custo não foi medido, como manda a §2 do PRD. A tela não terá quatro
cartões simétricos.

**D-07 · Escrevi em `package.json`, que não é meu.** O commit `a569ef4` registrou o apelido
`verificar-admin` no `package.json`. O §7 lista o que a sessão escreve — rotas, componentes,
módulo de dados, folha e o próprio arquivo de estado — e `package.json` não está na lista. A
S2 pediu e esperou pelo apelido dela; eu não pedi. Uma linha, conteúdo certo, nada a
desfazer, e a coordenação nivelou acrescentando os apelidos das outras duas em `1f2b8a4`. O
erro não é o conteúdo, é ter decidido sozinha num arquivo compartilhado depois de ter
cobrado exatamente isso das outras sessões.

**D-06 · `scripts/medidas.mjs` não é o que o §9 do PRD supõe.** O portão 4 pede «medidas de
pixel em `scripts/medidas.mjs`, nas duas visões». O arquivo tem 25 linhas e exporta duas
âncoras de commit — a medição de pixel mora nas `verificar-fase*.mjs`, que são território
proibido pelo §6 e exigem `out/`. Medi cada tela no navegador a 1440px e confirmei que a
visão app esconde a superfície (D-67): não há conteúdo de Admin para medir na segunda visão,
porque o layout de `(bastidor)` não o renderiza lá.

**D-05 · `meta.json` não conta o que o mapa mostra.** `cobertura.coordenadas.comCoordenada`
são 472 — as entidades com coordenada **própria**, o tamanho da tabela de referência. O mapa
posiciona **1.380**, porque ocorrência herda do espaço e espaço herda do município. A frase
«45% das coordenadas são centroide de país» é verdadeira sobre a tabela e falsa sobre o
mapa, onde a fatia é 29,1%. A A3 exibe as duas com o nome certo. Vale para quem citar
cobertura de coordenada em qualquer outra superfície.

**D-04 · As telas do Admin só existem na visão web.** O PRD descreve um layout «App.» para
cada uma das dez, mas o layout de `(bastidor)` esconde o conteúdo na visão app e mostra o
aviso de superfície (D-67) — as linhas «App.» do PRD não têm onde renderizar. Sigo o código,
como fazem Studio, Moderação e Observatório.
